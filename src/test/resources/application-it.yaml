spring:
  datasource:
    URL: jdbc:postgresql://localhost:5432/gerenciador-oficina
    username: postgres
    password: totvs@123

  jpa:
    show-sql: true
    properties:
      hibernate:
        default_schema: '"_IT"'
        '[generate_statistics]': true

  flyway:
    default-schema: _IT
    schemas: _IT